services:
  - type: web
    name: quiz-app
    runtime: node
    buildCommand: npm install && npm run build && cd server && npm install
    startCommand: node server/app.js
    envVars:
      - key: NODE_ENV
        value: production
